<UserControl x:Class="WpfApp9.View.ContactListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp9.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <DockPanel Grid.Row="0" >
            <Label Content="Name:"/>
            <TextBox  Margin="20, 0" HorizontalAlignment="Stretch" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>
        </DockPanel>

        <DockPanel Grid.Row="1" >
            <Label Content="Phone:"/>
            <TextBox  Margin="20, 0" HorizontalAlignment="Stretch" Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}"/>
        </DockPanel>
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="10">
            <Button Content="Добавить" Width="80" Margin="0,0,10,0" Command="{Binding AddCommand}"/>
            <Button Content="Удалить" Width="80" Margin="0,0,10,0" Command="{Binding DeleteCommand}"/>
            <Button Content="Редактировать" Width="80" Command="{Binding EditCommand}"/>
        </StackPanel>
        <DataGrid Grid.Row="3" AutoGenerateColumns="False" IsReadOnly="True"
    ItemsSource="{Binding Contacts}"
    SelectedItem="{Binding SelectedContact}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Имя" Binding="{Binding Path=Name}"/>
                <DataGridTextColumn Header="Телефон" Binding="{Binding Path=Phone}"/>
            </DataGrid.Columns>
        </DataGrid>

        <StatusBar Grid.Row="5">
            <Label Content="{Binding ErrorMsg}"/>
        </StatusBar>
    </Grid>
</UserControl>
